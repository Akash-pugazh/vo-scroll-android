name: Mobile CI

on:
  pull_request:
  push:
    branches: [ main, master ]

jobs:
  flutter-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - name: Regenerate Android host files for current Flutter
        run: flutter create --platforms=android .
      - name: Install deps
        run: flutter pub get
      - name: Analyze
        run: flutter analyze
      - name: Test
        run: flutter test
